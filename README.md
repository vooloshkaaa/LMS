# Lessons Management System (LMS)

Web-застосунок для керування навчальним центром англійської мови: студенти, викладачі, групи, розклад, відвідуваність, платежі та базова аналітика.

Проєкт реалізовано як SPA на React + TypeScript із маршрутизацією та централізованим станом через контексти.

## Основні можливості

- Авторизація користувачів із ролями: `admin`, `teacher`, `student`.
- Дашборд із загальною інформацією та ключовими показниками.
- Управління студентами: додавання, редагування, видалення, контроль балансу.
- Управління викладачами: профілі, спеціалізації, прив'язані групи.
- Управління групами: рівень, склад студентів, ліміт місць.
- Розклад занять: групові та індивідуальні уроки.
- Облік відвідуваності з автоматичним списанням вартості уроку.
- Фінансовий модуль: депозити та платежі за заняття.
- Аналітика: дохід, відвідуваність, навантаження викладачів.
- Сторінки для студентів: мої уроки, бронювання уроку, мій баланс.

## Маршрути застосунку

- `/login` - вхід у систему.
- `/dashboard` - головна інформаційна панель.
- `/students` - модуль студентів.
- `/teachers` - модуль викладачів.
- `/groups` - модуль груп.
- `/schedule` - розклад уроків.
- `/payments` - історія та облік платежів.
- `/analytics` - аналітичні графіки та метрики.
- `/attendance` - відмітка відвідуваності.
- `/my-lessons` - персональні уроки студента.
- `/book-lesson` - бронювання індивідуального уроку.
- `/my-balance` - баланс студента.

## Технологічний стек

- `React 18` + `TypeScript`
- `Vite` для збірки та dev-сервера
- `React Router` для клієнтської навігації
- `Tailwind CSS` для стилізації
- `Recharts` для графіків
- `React Hook Form` + `Zod` для форм і валідації
- `Sonner` для toast-сповіщень

## Архітектура стану

- `AuthContext`:
  - зберігає поточного користувача;
  - виконує `login/logout`;
  - зберігає сесію в `sessionStorage`.
- `LMSContext`:
  - зберігає колекції студентів, викладачів, груп, уроків, платежів, сповіщень;
  - надає CRUD-операції;
  - обробляє бізнес-логіку відвідуваності та змін балансу.

## Дані в поточній версії

Поточна реалізація використовує локальні тестові дані (`mockData`) без реального бекенду та БД. Це зручно для демонстрації функціоналу й захисту дипломного проєкту.

## 🚀 Покрокова інструкція для розробника

### 1) Необхідне програмне забезпечення

Перед почат роботи встановіть наступні програми:

#### **Node.js та npm**
```bash
# Завантажте та встановіть Node.js LTS версії (18+)
# https://nodejs.org/
# npm встановлюється автоматично з Node.js
```

#### **Git**
```bash
# Завантажте та встановіть Git
# https://git-scm.com/
# Для Windows: https://git-scm.com/download/win
# Для macOS: brew install git
# Для Linux: sudo apt install git (Ubuntu/Debian)
```

#### **Рекомендований редактор коду**
- **Visual Studio Code** (рекомендовано)
- Розширення для VS Code:
  - ES7+ React/Redux/React-Native snippets
  - TypeScript Importer
  - Tailwind CSS IntelliSense
  - Prettier - Code formatter
  - ESLint

### 2) Налаштування середовища розробки

#### **Перевірка встановлення**
```bash
# Перевірте версії встановлених програм
node --version  # має бути v18.0.0 або вище
npm --version   # має бути 8.0.0 або вище
git --version   # будь-яка актуальна версія
```

#### **Налаштування Git (один раз)**
```bash
git config --global user.name "Ваше Ім'я"
git config --global user.email "your.email@example.com"
```

### 3) Клонування репозиторію

```bash
# Клонуйте проект з GitHub
git clone https://github.com/vooloshkaaa/LMS.git

# Перейдіть в папку проекту
cd LMS
```

### 4) Встановлення та конфігурація залежностей

#### **Встановлення пакетів**
```bash
# Встановіть всі залежності з package.json
npm install

# Процес може зайняти 2-5 хвилин залежно від швидкості інтернету
```

#### **Перевірка встановлення**
```bash
# Перевірте, чи всі пакети встановлені коректно
npm list --depth=0
```

### 5) База даних

**На поточний момент проект не потребує налаштування бази даних**, оскільки використовує mock дані для демонстрації функціоналу.

#### **Планована інтеграція з Supabase (опціонально)**
```bash
# Якщо хочете підключити Supabase в майбутньому:
# 1. Створіть акаунт на https://supabase.com/
# 2. Створіть новий проект
# 3. Додайте змінні середовища:
#    SUPABASE_PROJECT_ID=your_project_id
#    SUPABASE_ANON_KEY=your_anon_key
# 4. Запустіть генерацію типів:
npm run types:supabase
```

### 6) Запуск проекту у режимі розробки

```bash
# Запустіть dev-сервер
npm run dev

# Сервер запуститься на http://localhost:5173
# Відкрийте цю адресу в браузері
```

#### **Перевірка успішного запуску**
- Відкрийте http://localhost:5173 у браузері
- Ви повинні побачити сторінку входу в систему
- Використовуйте тестові дані для входу:
  - Email: `admin@example.com`
  - Пароль: будь-який (мінімум 4 символи)

### 7) Базові команди та операції

#### **Основні скрипти**
```bash
# Запуск у режимі розробки з гарячою перезавантаженням
npm run dev

# Збірка проекту для production
npm run build

# Перегляд production-збірки локально
npm run preview

# Перевірка коду на відповідність стандартам
npm run lint

# Генерація TypeScript типів для Supabase
npm run types:supabase
```

#### **Корисні команди розробки**
```bash
# Оновлення залежностей до останніх версій
npm update

# Перевірка наявності оновлень
npm outdated

# Видалення node_modules та перевстановлення
rm -rf node_modules package-lock.json
npm install

# Запуск з конкретним портом (якщо 5173 зайнятий)
npm run dev -- --port 3000
```

### 8) Структура проекту для розробника

```
src/
├── components/     # Переиспользуемые UI-компоненти
├── contexts/       # React Context для стану додатку
├── data/          # Mock дані для розробки
├── hooks/         # Кастомні React хуки
├── lib/           # Утиліти та helper функції
├── pages/         # Сторінки додатку (маршрути)
├── types/         # TypeScript типи
├── App.tsx        # Головний компонент з роутингом
└── main.tsx       # Точка входу додатку
```

### 9) Поширені проблеми та рішення

#### **Проблема: "Port 5173 is already in use"**
```bash
# Знайдіть процес, що використовує порт
netstat -ano | findstr :5173  # Windows
lsof -i :5173                  # macOS/Linux

# Або запустіть на іншому порту
npm run dev -- --port 3000
```

#### **Проблема: "npm install fails"**
```bash
# Очистіть кеш npm
npm cache clean --force

# Видаліть та перевстановіть залежності
rm -rf node_modules package-lock.json
npm install
```

#### **Проблема: TypeScript помилки**
```bash
# Перевірте конфігурацію
npx tsc --noEmit

# Перевірте встановлення TypeScript
npm list typescript
```

### 10) Наступні кроки після запуску

1. **Ознайомтесь з кодом** - почніть з `src/App.tsx` та `src/pages/`
2. **Вивчіть структуру даних** - подивіться `src/types/lms.ts` та `src/data/mockData.ts`
3. **Запустіть тестові сценарії** - спробуйте увійти як різні користувачі
4. **Дослідіть компоненти** - подивіться `src/components/ui/` для UI елементів

---

## Запуск локально

### Швидкий старт (для досвідчених розробників)

```bash
git clone https://github.com/vooloshkaaa/LMS.git
cd LMS
npm install
npm run dev
```

### Детальні вимоги

- `Node.js` версії 18+ (рекомендовано LTS)
- `npm` версії 8+
- `Git` будь-якої актуальної версії

## Скрипти

- `npm run dev` - запуск у режимі розробки.
- `npm run build` - production-збірка.
- `npm run preview` - локальний перегляд production-збірки.
- `npm run lint` - перевірка коду ESLint.
- `npm run types:supabase` - генерація TypeScript-типів Supabase (за наявності налаштованого проєкту).

## Структура проєкту

```text
src/
  components/      # UI-компоненти
  contexts/        # AuthContext, LMSContext
  data/            # mock-дані
  pages/           # сторінки застосунку
  types/           # TypeScript-типи домену LMS
  App.tsx          # маршрути застосунку
```

## Можливі покращення

- Підключення реального бекенду 
- Повноцінна рольова авторизація на рівні маршрутів.
- Збереження даних у БД замість `mockData`.
- Інтеграція push/email-сповіщень.
- Розширена звітність (експорт у PDF/Excel).

## Призначення проєкту

Цей LMS створено як навчально-практичний дипломний проєкт для демонстрації:
- проєктування SPA-архітектури;
- організації доменної моделі навчального центру;
- реалізації базових бізнес-процесів (розклад, фінанси, відвідуваність);
- застосування сучасного frontend-стеку на TypeScript.
